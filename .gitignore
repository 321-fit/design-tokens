node_modules/

# Web output — regenerated on every build, not consumed directly
# (prototype copies build/css/tokens.css into its own repo)
build/

# Swift / Gradle local build
.build/
.swiftpm/
android/build/
android/.gradle/
local.properties

.DS_Store
*.DS_Store

# NOTE: Sources/FitUI/Tokens/Generated/ AND android/src/main/res/values/
# ARE committed — SwiftPM and Gradle consumers need them to resolve
# the library without running `npm run build` themselves.
# CI should verify `npm run build` leaves no diff (drift guard).
